export default function AdminStations() {
    return (
        <main style={{ padding: 24 }}>
            <h1>Stations (Admin)</h1>
            <p>Create/Moderate stations here (full version in earlier build).</p>
        </main>
    )
}